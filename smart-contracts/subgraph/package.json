{
  "name": "stardorm-eip8004-subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "Indexes IdentityRegistry, ReputationRegistry, and ValidationRegistry for agent discovery and reputation (Goldsky / Graph Node compatible).",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "graph build && goldsky subgraph deploy stardorm-eip8004/1.0.0 --path ."
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.95.0",
    "@graphprotocol/graph-ts": "0.36.0"
  }
}
