/** One-time checkout supports at most this many payers (you + up to 3 others). */
export const PAYMENT_MAX_PAYER_COUNT = 4;

/** Maximum payers besides a single “primary” slot — i.e. three additional wallets. */
export const PAYMENT_MAX_OTHER_PAYERS = 3;
