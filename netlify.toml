[build]
  command = "pnpm run build:app"
  publish = "app/dist/client"

[build.environment]
  NODE_VERSION = "22"

# TanStack Start SPA shell (see vite.config.ts spa mode)
[[redirects]]
  from = "/*"
  to = "/_shell.html"
  status = 200
